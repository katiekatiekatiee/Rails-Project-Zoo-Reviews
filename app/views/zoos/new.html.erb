<h1>Add a New Zoo by filling out the fields below:</h1>

<%= form_for @zoo do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %><br>

    <%= f.collection_select(:location_id, Location.all, :id, :name, { :prompt => "Select State" }) %>


    <%= f.submit %>
<% end %>